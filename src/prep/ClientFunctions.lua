--[=[
    @prop Client Client
    @within Udon

    The client's functions, only accessible by players, and other similar client-based runners.
    @client
]=]

return function(udon)
	-- Handle server functions.

	--[=[
        @class Client

        The class containing functions and properties that can only be used by the client.
    ]=]
	local Client = {}
end
