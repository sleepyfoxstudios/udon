--[=[
    @prop Server Server
    @within Udon

    The server functions, only accessible on the server.
    @server
]=]

return function(udon: any)
	-- Handle server functions.

	--[=[
        @class Server

        The class containing functions and properties that can only be used by the server.
    ]=]
	local Server = {}
end
